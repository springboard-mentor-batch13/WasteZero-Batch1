const MESSAGE_TYPE = {
  TEXT: 'TEXT',
  IMAGE: 'IMAGE',
  FILE: 'FILE',
  SYSTEM: 'SYSTEM'
};

const MESSAGE_TYPE_ARRAY = Object.values(MESSAGE_TYPE);

module.exports = { MESSAGE_TYPE, MESSAGE_TYPE_ARRAY };
