const DURATION_UNITS = {
  HOURS: 'hours',
  DAYS: 'days',
  WEEKS: 'weeks',
  MONTHS: 'months'
};

const DURATION_UNITS_ARRAY = Object.values(DURATION_UNITS);

module.exports = { DURATION_UNITS, DURATION_UNITS_ARRAY };
